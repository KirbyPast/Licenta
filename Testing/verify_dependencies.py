import torch
import cv2
import numpy as np
from sam2.build_sam import build_sam2
from PIL import Image

print(f"Pytorch version: {torch.__version__}")
print(f"OpenCV version: {cv2.__version__}")
print(f"Numpy versionL {np.__version__}")
